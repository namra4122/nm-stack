# NM Multiplexer

1. Understand how does the multiplexer works internally. checkout the codebase of ![Herdr](github.com/herdrdev/herdr) and ![tmux](github.com/tmux/tmux) understand how does the system works

2. Build the individual components:
   - [x] create the basic Inter-Process Communication (IPC) server and client
   - [ ] build the simple PTY shell
   - [ ] IPC server and client which reads the clients keyboard import and streams the raw and human-readable output back to the client
